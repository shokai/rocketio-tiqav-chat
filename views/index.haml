- content_for :html_foot do
  %script{:src => "/js/index.js"}

- content_for :header do
  %h1= app_name

%h2 create new chat
%div
  %input{:type => :text, :size => 30, :id => :text_chat_name}
  %input{:type => :button, :value => "go", :id => :btn_create_chat}
%h2 chats
%ul
- @channels.each do |c, count|
  %li
    %span
      %a{:href => "/#{c}"}= c
    %span (#{count} users)
